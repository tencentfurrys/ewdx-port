// Top-level build file. Versions pinned to mutually compatible, existing releases:
//   Android Gradle Plugin 8.5.2 (via settings.gradle pluginManagement)
//   <-> Gradle 8.7 (wrapper) <-> NDK 27.3.13750724 (installed)
// Intentionally empty: repositories live in settings.gradle
// (dependencyResolutionManagement, FAIL_ON_PROJECT_REPOS).
